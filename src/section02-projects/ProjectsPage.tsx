import SectionHeader from "../section-features/SectionHeader";

export default function ProjectsPage() {
    return (
        <div id="projects">
            <SectionHeader className="projects" entryNumber={2} titleLabel="Projects (WIP)" />
        </div>
    );
}