import SectionHeader from "../section-features/SectionHeader";

export default function ExperiencesPage() {
    return (
        <>
            <SectionHeader entryNumber={3} titleLabel="Experiences" />
        </>
    );
}