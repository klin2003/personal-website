import SectionHeader from "../section-features/SectionHeader";

export default function MainPage() {
    return (
        <>
            <SectionHeader entryNumber={0} titleLabel="Main" />
        </>
    );
}