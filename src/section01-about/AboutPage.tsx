import SectionHeader from "../section-features/SectionHeader";

export default function AboutPage() {
    return (
        <div id="about">
            <SectionHeader entryNumber={1} titleLabel="About" />
        </div>
    );
}